export default {
  async fetch(request) {
    const url = new URL(request.url);
    const path = url.pathname;

    const MAIN = "https://animeinpost.pages.dev";

    const SERIES_1 = "https://animeinpost-1.pages.dev";
    const SERIES_2 = "https://animeinpost-2.pages.dev";
    const SERIES_3 = "https://animeinpost-3.pages.dev";
    const SERIES_4 = "https://animeinpost-4.pages.dev";
    const SERIES_5 = "https://animeinpost-5.pages.dev";
    const SERIES_6 = "https://animeinpost-6.pages.dev";
    const SERIES_7 = "https://animeinpost-7.pages.dev";
    const SERIES_8 = "https://animeinpost-8.pages.dev";
    const SERIES_9 = "https://animeinpost-9.pages.dev";
    const SERIES_10 = "https://animeinpost-10.pages.dev";

    // Redirect /series and /series/<series> to trailing slash
    // Do NOT redirect chapter URLs (/read/)
    if (
      !path.endsWith("/") &&
      !path.includes("/read/") &&
      (
        path === "/series" ||
        path.startsWith("/series/")
      )
    ) {
      return Response.redirect(
        new URL(path + "/" + url.search, url.origin),
        301
      );
    }

    let target = MAIN;

    // ============================================================
    // MAIN PROJECT
    // ============================================================
    if (
      path === "/" ||
      path.startsWith("/assets/") ||
      path.startsWith("/meta/") ||
      path === "/robots.txt" ||
      path === "/sitemap.xml" ||
      path === "/favicon.ico" ||
      path === "/manifest.json"
    ) {
      target = MAIN;
    }

    // ============================================================
    // PAGE 1
    // https://animeinpost-1.pages.dev
    // ============================================================
    else if (
      path === "/series/" ||
      path.startsWith("/series/cont/") ||
      path.startsWith("/series/eleceed/") ||
      path.startsWith("/series/absolute-sword-sense/") ||
path.startsWith("/series/dragon-devouring-mage/") ||
path.startsWith("/series/dungeon-odyssey/") ||
path.startsWith("/series/kidnapped-dragons/") ||
path.startsWith("/series/return-of-the-apocalypse-class-death-knight/") ||
path.startsWith("/series/return-of-the-sss-class-ranker/") ||
path.startsWith("/series/star-embracing-swordmaster/") ||
path.startsWith("/series/the-hero-starts-over-at-the-academy/") ||
path.startsWith("/series/the-last-adventurer/") ||
path.startsWith("/series/trait-hoarder/") ||
      path.startsWith("/series/30-years-since-the-prologue/") ||
      path.startsWith("/series/a-painter-who-draws-dungeons/") ||
      path.startsWith("/series/a-regressors-tale-of-cultivation/") ||
      path.startsWith("/series/academys-genius-swordmaster/") ||
      path.startsWith("/series/boruto-two-blue-vortex/") ||
      path.startsWith("/series/eternally-regressing-knight/") ||
      path.startsWith("/series/god-of-martial-arts/") ||
      path.startsWith("/series/got-dropped-into-a-ghost-story-still-gotta-work/") ||
      path.startsWith("/series/i-was-mistaken-as-a-monstrous-genius-actor/") ||
      path.startsWith("/series/invincible-in-the-apocalypse/") ||
      path.startsWith("/series/is-it-bad-that-the-main-characters-a-roleplayer/") ||
      path.startsWith("/series/magic-emperor/") ||
      path.startsWith("/series/manager-kim/") ||
      path.startsWith("/series/my-slain-dragon-bride/") ||
      path.startsWith("/series/perhaps-invincible/") ||
      path.startsWith("/series/revenge-of-the-iron-blooded-sword-hound/") ||
      path.startsWith("/series/solo-max-level-newbie/") ||
      path.startsWith("/series/star-instructor-master-baek/") ||
      path.startsWith("/series/the-executioner/") ||
      path.startsWith("/series/the-extras-academy-survival-guide/") ||
      path.startsWith("/series/the-greatest-estate-developer/") ||
      path.startsWith("/series/the-max-level-players-100th-regression/") ||
      path.startsWith("/series/i-am-the-fated-villain/") ||
      path.startsWith("/series/lookism/") ||
      path.startsWith("/series/one-piece-manga/") ||
      path.startsWith("/series/one-piece/") ||
      path.startsWith("/series/a-million-sub-gym-influencer-becomes-a-webtoon-loser/") ||
      path.startsWith("/series/absolute-necromancer/") ||
      path.startsWith("/series/bloodhounds-regression-instinct/") ||
      path.startsWith("/series/bones/") ||
      path.startsWith("/series/boundless-necromancer/") ||
      path.startsWith("/series/heavenly-inquisition-sword/") ||
      path.startsWith("/series/insanely-talented-player/") ||
      path.startsWith("/series/male-god-your-persona-has-collapsed/") ||
      path.startsWith("/series/my-fake-girlfriends-are-using-me-as-a-shield/") ||
      path.startsWith("/series/sss-class-suicide-hunter/") ||
      path.startsWith("/series/superhuman-battlefield/") ||
      path.startsWith("/series/orv/") ||
      path.startsWith("/series/orv-manhwa/") ||
      path.startsWith("/series/side/") ||
      path.startsWith("/series/solo-leveling/") ||
      path.startsWith("/series/solo-leveling-ragnarok-manhwa/") ||
      path.startsWith("/series/the-beginning-after-the-end/") ||
      path.startsWith("/series/tower-of-god/") ||
      path.startsWith("/series/twate-manhwa/") ||
      path.startsWith("/series/urek-mazino/") ||
      path.startsWith("/series/logging-10000-years-into-the-future/") ||
      path.startsWith("/series/nano-machine/") ||
      path.startsWith("/series/pick-me-up-infinite-gacha/") ||
      path.startsWith("/series/regressor-instruction-manual/") ||
      path.startsWith("/series/the-priest-of-corruption/") ||
      path.startsWith("/series/the-regressed-son-of-a-duke-is-an-assassin/") ||
      path.startsWith("/series/the-swordmasters-youngest-son/") ||
      path.startsWith("/series/top-tier-providence/")
    ) {
      target = SERIES_1;
    }

    // ============================================================
    // PAGE 2
    // https://animeinpost-2.pages.dev
    // ============================================================
    else if (
      path.startsWith("/series/a-villains-will-to-survive/") ||
      
      path.startsWith("/series/the-academys-sashimi-sword-master/") ||
path.startsWith("/series/i-killed-an-academy-player/") ||
path.startsWith("/series/breakers/") ||
path.startsWith("/series/solo-farming-in-the-tower/") ||
path.startsWith("/series/what-a-bountiful-harvest-demon-lord/") ||
path.startsWith("/series/terminally-ill-genius-dark-knight/") ||

      path.startsWith("/series/the-knight-king-who-returned-with-a-god/")
    ) {
      target = SERIES_2;
    }

    // ============================================================
    // PAGE 3
    // https://animeinpost-3.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_3;
    }

    // ============================================================
    // PAGE 4
    // https://animeinpost-4.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_4;
    }

    // ============================================================
    // PAGE 5
    // https://animeinpost-5.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_5;
    }

    // ============================================================
    // PAGE 6
    // https://animeinpost-6.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_6;
    }

    // ============================================================
    // PAGE 7
    // https://animeinpost-7.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_7;
    }

    // ============================================================
    // PAGE 8
    // https://animeinpost-8.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_8;
    }

    // ============================================================
    // PAGE 9
    // https://animeinpost-9.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_9;
    }

    // ============================================================
    // PAGE 10
    // https://animeinpost-10.pages.dev
    // ADD SERIES PATHS HERE
    // ============================================================
    else if (
      false
      // Example:
      // path.startsWith("/series/example/")
    ) {
      target = SERIES_10;
    }

    const targetUrl = new URL(path + url.search, target);

    const response = await fetch(
      new Request(targetUrl, request)
    );

    // Serve custom 404 page from Main project
    if (response.status === 404) {
      const custom404 = await fetch(
        "https://animeinpost.pages.dev/404.html"
      );

      return new Response(custom404.body, {
        status: 404,
        headers: custom404.headers,
      });
    }

    return response;
  }
};